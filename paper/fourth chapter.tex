\chapter{Implementation}\label{ch:implementation}

  The previous chapter introduced the design of SMILE from four perspectives: the extension of the meta schema to M-Model+, the engine architecture with its six-layer
  structure and five-phase pipeline, the operation taxonomy, and the dual grammar design.  This chapter describes the concrete implementation of these concepts.\footnote{The complete source code is available at
  \url{https://github.com/baobaodaren0918/SMILE-3.0}. During implementation, Claude Code was used in part for the ANTLR~4 grammar implementation. The workflow, core logic, data flow, and all architectural design decisions were developed independently by the author.} The structure follows the six architectural layers defined in \autoref{sec:architecture}. \autoref{sec:tech-stack} gives an overview of the technologies used.
  \autoref{sec:meta-impl} explains the implementation of M-Model+ as the central data structure, including the type mappings between the four database systems.
  \autoref{sec:parser-impl} covers the grammar processing chain from ANTLR~4 code generation and automatic grammar recognition to the listener-based generation of the
  operation list. \autoref{sec:transformer} describes the Schema Transformer, the central component of the core layer that receives the parsed operation list and applies each operation to the M-Model+ database object through dedicated handlers, transforming Meta Schema V1 into Meta Schema V2 as defined in \autoref{sec:operation-taxonomy}.  \autoref{sec:adapters} documents the four adapters for reverse and forward engineering. \autoref{sec:validation} presents the two-layer
  validation, and \autoref{sec:user-interface} describes the user interface, which makes the entire migration and evolution process visible.

\section{Technology Stack}\label{sec:tech-stack}
\subsection{Programming Language}\label{subsec:lang-python}

  The entire implementation of SMILE is realized in Python. ANTLR~4~\cite{parr2013definitive} provides an official Python target, so that Lexer, Parser, and Listener
  classes can be generated directly from the \texttt{.g4} grammar files without introducing an additional language binding layer. Furthermore, Python's
  \texttt{dataclass} mechanism is particularly well suited for modeling M-Model+, since data classes such as \texttt{Property}, \texttt{EntityType}, or
  \texttt{Reference} can be defined declaratively with field names, types, and default values. As a dynamically typed language, Python also enables rapid prototype development~\cite{zeller2022academic}, which matches the character of a research prototype.

\subsection{Web Interface}\label{subsec:tech-web}
The visualization of migration and evolution results is provided through a web-based interface built on Python's standard library HTTP server (\texttt{http.server}).    No external web framework is required. The frontend uses plain HTML, CSS, and JavaScript without additional frameworks. The interface allows users to compare source
  and target schemas side by side, trace the step-by-step transformation from Meta Schema V1 to Meta Schema V2, and inspect the validation results of both validation
  layers.
\subsection{Schema File Processing}\label{subsec:tech-file-driven}

  A deliberate design decision is that SMILE does not connect to running database instances but operates exclusively on schema definition files. PostgreSQL schemas are
  parsed from SQL DDL (\texttt{.sql}), MongoDB schemas from JSON Schema documents (\texttt{.json}), Neo4j schemas from Cypher files (\texttt{.cypher}), and Cassandra
  schemas from CQL DDL (\texttt{.cql}). This scoping decision restricts SMILE to schema-level transformations, covering structural migration and evolution without
  addressing data migration or runtime connectivity. Each input format imposes distinct parsing constraints. SQL DDL uses a declarative, table-based syntax with typed
   column definitions, inline constraints, and foreign key references. MongoDB schemas follow a recursive JSON structure with nested \texttt{properties} and
  \texttt{required} arrays conforming to the JSON Schema specification.  Neo4j does not yet provide a generally available native schema definition language~\cite{angles2023pg}\footnote{Starting with Neo4j 2026.02 (released on February 10, 2026), graph types
  were introduced in Cypher~25 as an Enterprise Preview feature, providing a DDL-like mechanism for defining graph schemas. Since this feature remains in Preview status   rather than general availability, SMILE uses structured comment annotations instead. See \url{https://neo4j.com/docs/cypher-manual/current/schema/graph-types/}.}, so
  schemas are reconstructed from structured comment annotations in Cypher files that encode node types, relationship types, property names, and data types, as shown in    \autoref{lst:neo4j-schema-example}.
  Cassandra CQL DDL extends standard DDL syntax with partition key and clustering key declarations inside composite \texttt{PRIMARY KEY} clauses.

  \begin{lstlisting}[basicstyle=\small\ttfamily, caption={Neo4j schema definition via structured comment annotations}, label={lst:neo4j-schema-example}]
  // Node: categories
  // Key: category_id
  // Properties: category_id (string), category_name (string),
  //             description (string)

  // Node: products
  // Key: product_id
  // Properties: product_id (string), product_name (string),
  //             unit_price (double), units_in_stock (integer),
  //             discontinued (boolean), quantity_per_unit (string)

  // Relationship: CONTAINS (orders -> products)
  // Properties: unit_price (double), quantity (integer),
  //             discount (double)
  \end{lstlisting}

  \subsection{Module Overview}\label{subsec:tech-project-structure}

  \autoref{tab:project-modules} maps the main source files to the architectural layers defined in \autoref{sec:architecture} and to the sections of this chapter.



\section{M-Model+ Implementation}\label{sec:meta-impl}

Chapter~3 defines M-Model+ as the canonical intermediate representation that connects the four database paradigms. For this design to function, a data structure is needed that can accommodate relational tables, nested documents, graph nodes, and wide-column tables equally. This section describes the concrete implementation of this data structure and its associated type mapping mechanism in the module \texttt{Schema/unified\_meta\_schema.py}. The adapters instantiate these classes when parsing a source schema into M-Model+. The Schema Transformer then executes SMILE operations directly on these objects. After transformation, the validation layer serializes the resulting state to JSON and compares it against the expected target.

\subsection{Data Structures}\label{subsec:meta-data-structures}

  Chapter~3 defines the components of M-Model+ at the conceptual level. This subsection describes their concrete implementation in
  \texttt{Schema/unified\_meta\_schema.py}. \autoref{fig:meta-class-hierarchy} illustrates the complete class hierarchy.

 The entire module is built on Python's \texttt{@dataclass} decorator, so that each class can be defined with declarative field definitions instead of manually written
  constructors. Beyond construction, every class also implements \texttt{to\_dict()} and \texttt{from\_dict()} methods, since the system needs to serialize schema states
   at multiple points. The adapters use \texttt{from\_dict()} to rebuild objects from JSON during reverse engineering. The web interface calls \texttt{to\_dict()} to
  display schema states. The validation layer calls \texttt{to\_dict()} on both the migration result and the expected target, then compares the two outputs to detect      differences.

  As shown in the upper part of \autoref{fig:meta-class-hierarchy}, \texttt{Database} is the outermost container and stores all entities in a \texttt{Dict[str,
  EntityType]}, where the full path serves as key (e.g.\ \texttt{"products"} or \texttt{"orders.customer"}). A dictionary is used instead of a list because the Schema Transformer needs to look up entities by name. For example, executing \texttt{NEST categories IN               products.category} requires access to both \texttt{categories} and \texttt{products} at the same time.

  The class \texttt{EntityType}, shown in the second level of the figure, is the most complex in the module and contains three lists. Each \texttt{Property} object in
  the \texttt{properties} list comprises a field name, a data type, and two boolean flags \texttt{is\_key} and \texttt{is\_optional}. These two flags are extensions of
  M-Model+ over the original M-Model and enable cross-paradigm key and null-value queries without traversing the constraint list. The \texttt{constraints} list
  distinguishes \texttt{UniqueConstraint} (primary and unique keys) and \texttt{ForeignKeyConstraint} (foreign keys). Both reference \texttt{Property} objects via their
  \texttt{meta\_id} rather than holding direct object references. This indirect referencing scheme ensures that constraint relationships do not become invalid when a
  property is renamed.

  The abstract base class \texttt{Relationship} and its three subtypes, shown in the third level of the figure, form the core of cross-paradigm migration. All three
  share the fields \texttt{cardinality} and \texttt{is\_optional} (whose values are determined by the \texttt{Cardinality} enum shown on the right side of the figure),
  but each carries its own fields for the specific relationship semantics of its paradigm. This separation allows operation handlers to work directly with the correct
  subtype. For example, \texttt{NEST} creates an \texttt{Embedded} relationship and removes the original \texttt{Reference}, while \texttt{TRANSFORM} converts an
  \texttt{EntityType} between \texttt{VERTEX} and \texttt{EDGE}. If all three relationship types were represented by a single class, these handlers would need extensive
  conditional logic to distinguish which type of relationship is being processed.

  The \texttt{PKTypeEnum} shown at the lower left of the figure and the \texttt{Cardinality} enum shown on the right are connected via dashed lines to the classes that
  use their values. The five values of \texttt{PKTypeEnum} allow the \texttt{CAST\_CONSTRAINT} operation to convert between key types of different paradigms, for example
   from PostgreSQL's \texttt{SIMPLE} to Cassandra's \texttt{PARTITION}. The four values of \texttt{Cardinality} refine the original M-Model's binary
  \texttt{ONE}/\texttt{MANY}, so that the \texttt{RECARD} operation can precisely adjust the cardinality of a relationship.
\begin{figure}[htbp]                                                                                                                                                       \centering
    \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}[                                                                                                                                                       font=\scriptsize,
      cls/.style={draw, rounded corners=2pt, align=left, inner sep=5pt},                                                                                                       enu/.style={draw, dashed, rounded corners=2pt, align=left, inner sep=5pt,
                  fill=purple!8},                                                                                                                                              arr/.style={-{Stealth[length=1.8mm]}, semithick},
      lbl/.style={font=\scriptsize, fill=white, inner sep=1pt},
    ]

    %% ===== Database =====
    \node[cls, fill=blue!10, minimum width=12cm] (db) at (0, 0)
      {\textbf{Database}\\
       \texttt{entity\_types: Dict[str, EntityType]}};

    %% ===== EntityType =====
    \node[cls, fill=green!10, minimum width=12cm] (et) at (0, -2.5)
      {\textbf{EntityType}\\
       \texttt{object\_name: List[str]}\\
       \texttt{entity\_kind: EntityKind}\\
       \texttt{properties: List[Property]}\\
       \texttt{constraints: List[Constraint]}\\
       \texttt{relationships: List[Relationship]}};

    %% DB -> ET
    \draw[arr] (db.south) -- node[lbl, right] {0..*} (et.north);

    %% ===== EntityKind enum =====
    \node[enu, minimum width=3.2cm] (ek) at (8.5, -2.5)
      {\textbf{EntityKind}\\
       TABLE\\
       DOCUMENT\\
       EMBEDDED\\
       VERTEX\\
       EDGE\\
       WIDE\_COLUMN\_TABLE};

    %% ET -> EntityKind
    \draw[dashed, ->, gray] (et.east) -- (ek.west);

    %% ===== Property =====
    \node[cls, fill=yellow!15, minimum width=3.8cm, minimum height=2.2cm] (prop) at (-5.5, -7.0)
      {\textbf{Property}\\
       \texttt{attr\_name: str}\\
       \texttt{data\_type: DataType}\\
       \texttt{is\_key: bool}\\
       \texttt{is\_optional: bool}};

    %% ===== Constraint =====
    \node[cls, fill=orange!12, minimum width=3.8cm, minimum height=2.2cm] (con) at (0, -7.0)
      {\textbf{Constraint} (ABC)\\
       ~\\
       \texttt{UniqueConstraint}\\
       \quad \texttt{is\_primary\_key}\\
       \quad \texttt{unique\_properties}\\
       ~\\
       \texttt{ForeignKeyConstraint}\\
       \quad \texttt{foreign\_key\_properties}};

    %% ===== Relationship =====
    \node[cls, fill=red!8, minimum width=3.8cm, minimum height=2.2cm] (rel) at (5.5, -7.0)
      {\textbf{Relationship} (ABC)\\
       \texttt{cardinality: Cardinality}\\
       \texttt{is\_optional: bool}};

    %% ET -> three children via fork
    \coordinate (etfork) at (0, -5.0);
    \draw[semithick] (et.south) -- (etfork);
    \draw[arr] (etfork) -| node[lbl, pos=0.8, above] {0..*} (prop.north);
    \draw[arr] (etfork) -- node[lbl, right] {0..*} (con.north);
    \draw[arr] (etfork) -| node[lbl, pos=0.8, above] {0..*} (rel.north);

    %% ===== PKTypeEnum =====
    \node[enu, minimum width=3.0cm] (pk) at (-5.5, -10.8)
      {\textbf{PKTypeEnum}\\
       SIMPLE\\
       PARTITION\\
       CLUSTERING\\
       NODE\_KEY\\
       DOCUMENT\_ID};

    %% Constraint -> PKTypeEnum
    \draw[dashed, ->, gray] (con.south) -- ++(0, -0.6) -| (pk.north);

    %% ===== Reference =====
    \node[cls, fill=red!15, minimum width=3.5cm, minimum height=2.8cm] (ref) at (2.5, -11.5)
      {\textbf{Reference}\\
       ~\\
       \texttt{ref\_name: str}\\
       \texttt{refs\_to: str}\\
       \texttt{edge\_properties: List}\\
       ~\\
       \textit{(PostgreSQL FK)}};

    %% ===== Embedded =====
    \node[cls, fill=blue!12, minimum width=3.5cm, minimum height=2.8cm] (emb) at (6.0, -11.5)
      {\textbf{Embedded}\\
       ~\\
       \texttt{aggr\_name: str}\\
       \texttt{aggregates: str}\\
       ~\\
       ~\\
       \textit{(MongoDB nesting)}};

    %% ===== Edge =====
    \node[cls, fill=green!12, minimum width=3.5cm, minimum height=2.8cm] (edge) at (9.5, -11.5)
      {\textbf{Edge}\\
       ~\\
       \texttt{rel\_type\_name: str}\\
       \texttt{source\_entity: str}\\
       \texttt{target\_entity: str}\\
       ~\\
       \textit{(Neo4j edge)}};

    %% Inheritance: orthogonal fork from Relationship
    \coordinate (relfork) at (5.5, -9.5);
    \draw[semithick] (rel.south) -- (relfork);
    \draw[semithick] (relfork) -- (2.5, -9.5);
    \draw[semithick] (relfork) -- (9.5, -9.5);
    \draw[-{Triangle[open]}, semithick] (2.5, -9.5) -- (ref.north);
    \draw[-{Triangle[open]}, semithick] (6.0, -9.5) -- (emb.north);
    \draw[-{Triangle[open]}, semithick] (9.5, -9.5) -- (edge.north);

    %% ===== Cardinality enum =====
    \node[enu, minimum width=2.8cm] (card) at (9.5, -7.0)
      {\textbf{Cardinality}\\
       0..1 (ZERO\_TO\_ONE)\\
       1..1 (ONE\_TO\_ONE)\\
       0..n (ZERO\_TO\_MANY)\\
       1..n (ONE\_TO\_MANY)};

    %% Relationship -> Cardinality
    \draw[dashed, ->, gray] (rel.east) -- (card.west);

    \end{tikzpicture}
    }% end resizebox
    \caption{M-Model+ class hierarchy as implemented in \texttt{unified\_meta\_schema.py}}
    \label{fig:meta-class-hierarchy}
  \end{figure}

\subsection{Type Mapping Implementation}\label{subsec:meta-type-mapping-impl}

Beyond structural unification, cross-paradigm migration requires data type translation. The same string type is called \texttt{VARCHAR} in PostgreSQL, \texttt{string}   in MongoDB and Neo4j, and \texttt{TEXT} in Cassandra. As described in \autoref{subsec:meta-type-mappings}, M-Model+ addresses this through a hub-and-spoke architecture   at the type level (\autoref{fig:type-mapping-hub}).
  \begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
      font=\scriptsize,
      box/.style={draw, rounded corners=2pt, minimum height=0.6cm,
                  align=center, inner sep=4pt},
      arr/.style={-{Stealth[length=1.8mm]}, semithick},
    ]
      %% Source databases (left)
      \node[box, fill=blue!12, minimum width=2.8cm] (pg1) at (-5, 1.5) {PostgreSQL};
      \node[box, fill=green!12, minimum width=2.8cm] (mg1) at (-5, 0.5) {MongoDB};
      \node[box, fill=yellow!12, minimum width=2.8cm] (nj1) at (-5, -0.5) {Neo4j};
      \node[box, fill=red!10, minimum width=2.8cm] (ca1) at (-5, -1.5) {Cassandra};

      %% Hub
      \node[box, fill=gray!15, minimum width=3.2cm, minimum height=2.8cm,
            line width=1.5pt] (hub) at (0, 0)
        {\textbf{PrimitiveType}\\
         \texttt{STRING}\\
         \texttt{INTEGER}\\
         \texttt{DOUBLE}\\
         \texttt{BOOLEAN}\\
         \texttt{DATE}\\
         \texttt{UUID}\\
         \texttt{\ldots}};

      %% Target databases (right)
      \node[box, fill=blue!12, minimum width=2.8cm] (pg2) at (5, 1.5) {PostgreSQL};
      \node[box, fill=green!12, minimum width=2.8cm] (mg2) at (5, 0.5) {MongoDB};
      \node[box, fill=yellow!12, minimum width=2.8cm] (nj2) at (5, -0.5) {Neo4j};
      \node[box, fill=red!10, minimum width=2.8cm] (ca2) at (5, -1.5) {Cassandra};

      %% RE arrows: all converge to hub center-left
      \coordinate (hubL) at (hub.west);
      \draw[arr] (pg1.east) -- (hubL);
      \draw[arr] (mg1.east) -- (hubL);
      \draw[arr] (nj1.east) -- (hubL);
      \draw[arr] (ca1.east) -- (hubL);

      %% FE arrows: all diverge from hub center-right
      \coordinate (hubR) at (hub.east);
      \draw[arr] (hubR) -- (pg2.west);
      \draw[arr] (hubR) -- (mg2.west);
      \draw[arr] (hubR) -- (nj2.west);
      \draw[arr] (hubR) -- (ca2.west);

      %% Labels
      \node[font=\scriptsize\itshape] at (-5, -2.4) {Reverse Engineering};
      \node[font=\scriptsize\itshape] at (0, -2.4) {(Hub)};
      \node[font=\scriptsize\itshape] at (5, -2.4) {Forward Engineering};
    \end{tikzpicture}
    \caption{Hub-and-spoke type mapping with \texttt{PrimitiveType} as intermediate representation}
    \label{fig:type-mapping-hub}
  \end{figure}

The class \texttt{TypeMappings} in \texttt{unified\_meta\_schema.py} defines two dictionaries per database system, one for each direction. Eight dictionaries in total
  cover all type translations across the four paradigms. Two additional display dictionaries provide formatted type names for the web interface.    \autoref{lst:type-mapping-impl} shows an excerpt of the PostgreSQL mappings. When the
  module is loaded, the four forward engineering dictionaries are registered in a global \texttt{\_NATIVE\_TYPE\_REGISTRY}, indexed by \texttt{DatabaseType}. Every
  \texttt{PrimitiveDataType} exposes a \texttt{to\_native(db)} method that retrieves the target type from this registry. This allows any property type to be translated
  into the native type string of any target system with a single call, without the caller needing to know which mapping rules apply.


  \begin{lstlisting}[language=Python, basicstyle=\small\ttfamily, caption={PostgreSQL type mappings in \texttt{TypeMappings} (excerpt)}, label={lst:type-mapping-impl}]
  class TypeMappings:
      # RE: native SQL type -> PrimitiveType
      POSTGRESQL_TO_PRIMITIVE = {
          'VARCHAR':          PrimitiveType.STRING,
          'INTEGER':          PrimitiveType.INTEGER,
          'DOUBLE PRECISION': PrimitiveType.DOUBLE,
          'BYTEA':            PrimitiveType.BINARY,
          ...
      }
      # FE: PrimitiveType -> native SQL type
      PRIMITIVE_TO_POSTGRESQL = {
          PrimitiveType.STRING:    'VARCHAR',
          PrimitiveType.INTEGER:   'INTEGER',
          PrimitiveType.DOUBLE:    'DOUBLE PRECISION',
          PrimitiveType.OBJECT_ID: 'VARCHAR',  # fallback
          ...
      }
  \end{lstlisting}


For types without a counterpart in the target system, the forward engineering dictionaries define fallback values, for example \texttt{OBJECT\_ID} maps to
  \texttt{VARCHAR} in PostgreSQL. Beyond primitive types, the module also implements four composite data type classes (\texttt{ListDataType}, \texttt{SetDataType},
  \texttt{MapDataType}, \texttt{TupleDataType}), each with its own \texttt{to\_native()} method that produces the paradigm-specific format, for example
  \texttt{ListDataType} exports \texttt{INTEGER[]} in PostgreSQL, \texttt{list<INT>} in Cassandra, and \texttt{List<integer>} in Neo4j. As a result, both primitive and composite types can be correctly exported to any of the four target systems.


\section{Grammar and Parsing}\label{sec:parser-impl}

  This section describes how SMILE scripts are processed from source text to executable operations. As introduced in \autoref{subsec:grammar-framework}, SMILE uses
  ANTLR~4~\cite{parr2013definitive} for syntax analysis. \autoref{fig:antlr-files} provides an overview of the complete processing chain. The ANTLR tool generates Lexer,
   Parser, and Listener base classes from the two \texttt{.g4} grammar files (\autoref{subsec:antlr-codegen}). These generated classes are then selected at runtime by
  \texttt{parser\_factory.py} based on the script file extension (\autoref{subsec:parser-factory}). The custom listener classes in \texttt{smile\_listeners.py} inherit
  from the generated base classes, walk the parse tree, and produce an ordered list of \texttt{Operation} objects that the Schema Transformer can execute                  (\autoref{subsec:listeners}).
  \begin{figure}[t!]
      \centering
      \begin{tikzpicture}[
        font=\scriptsize,
        box/.style={draw, rounded corners=2pt, minimum height=0.6cm,
                    align=center, inner sep=4pt},
        gen/.style={draw, rounded corners=2pt, minimum height=0.6cm,
                    align=center, inner sep=4pt, fill=gray!15},
        arr/.style={-{Stealth[length=1.8mm]}, semithick},
        lbl/.style={font=\scriptsize, fill=white, inner sep=1pt},
      ]

      %% Row 1: .g4 grammar files
      \node[box, fill=blue!12, minimum width=3.2cm] (g4s) at (-2.5, 0)
        {\texttt{SMILE\_Specific.g4}};
      \node[box, fill=blue!12, minimum width=3.2cm] (g4g) at (2.5, 0)
        {\texttt{SMILE\_Generalized.g4}};

      %% Row 2: ANTLR4 tool
      \node[box, fill=orange!15, minimum width=7.0cm] (antlr) at (0, -1.4)
        {ANTLR~4 Code Generator};

      %% Row 3: Generated files (two columns)
      \node[gen, minimum width=3.2cm] (lexS) at (-2.5, -3.2)
        {Lexer (.py)\\Parser (.py)\\Listener Base (.py)};
      \node[gen, minimum width=3.2cm] (lexG) at (2.5, -3.2)
        {Lexer (.py)\\Parser (.py)\\Listener Base (.py)};

      %% Row 4: parser_factory.py
      \node[box, fill=green!12, minimum width=7.0cm] (pf) at (0, -5.4)
        {\texttt{parser\_factory.py}\\detects \texttt{.smile} / \texttt{.smile\_gen}};

      %% Row 5: smile_listeners.py
      \node[box, fill=yellow!15, minimum width=7.0cm] (sl) at (0, -7.2)
        {\texttt{smile\_listeners.py}\\
         \texttt{SMILESpecificListener} /
         \texttt{SMILEGeneralizedListener}};

      %% Row 6: Output
      \node[box, fill=red!10, minimum width=7.0cm] (ops) at (0, -8.8)
        {List of \texttt{Operation} objects};

      %% Arrows: g4 -> antlr
      \draw[arr] (g4s.south) -- (antlr.north -| g4s.south);
      \draw[arr] (g4g.south) -- (antlr.north -| g4g.south);

      %% Arrows: antlr -> generated (with label on arrow)
      \draw[arr] (antlr.south -| lexS.north) -- node[lbl, right, font=\scriptsize\itshape]
        {generated by ANTLR~4} (lexS.north);
      \draw[arr] (antlr.south -| lexG.north) -- node[lbl, left, font=\scriptsize\itshape]
        {generated by ANTLR~4} (lexG.north);

      %% Arrows: generated -> parser_factory
      \draw[arr] (lexS.south) -- ++(0, -0.5) -| ([xshift=-1.5cm]pf.north);
      \draw[arr] (lexG.south) -- ++(0, -0.5) -| ([xshift=1.5cm]pf.north);

      %% Arrow: parser_factory -> listeners
      \draw[arr] (pf.south) -- (sl.north);

      %% Inherits: orthogonal dashed arrows (left)
      \draw[-{Stealth[length=1.5mm]}, dashed, gray, rounded corners=3pt]
        (lexS.west) -- (-4.5, -3.2) -- (-4.5, -7.2) -- (sl.west);
      \node[lbl, font=\scriptsize\itshape, rotate=90] at (-4.5, -5.2) {inherits};

      %% Inherits: orthogonal dashed arrows (right)
      \draw[-{Stealth[length=1.5mm]}, dashed, gray, rounded corners=3pt]
        (lexG.east) -- (4.5, -3.2) -- (4.5, -7.2) -- (sl.east);
      \node[lbl, font=\scriptsize\itshape, rotate=-90] at (4.5, -5.2) {inherits};

      %% Arrow: listeners -> operations
      \draw[arr] (sl.south) -- (ops.north);

      \end{tikzpicture}
      \caption{ANTLR~4 code generation and file structure in the SMILE project}
      \label{fig:antlr-files}
  \end{figure}


  \subsection{ANTLR4 Code Generation}\label{subsec:antlr-codegen}

  Each \texttt{.g4} grammar file is processed by the ANTLR tool to produce three Python files. \autoref{fig:antlr-codegen-detail} shows all six generated files for both
  grammars. On the Specific side, the tool produces \texttt{SMILE\_SpecificLexer.py}, \texttt{SMILE\_SpecificParser.py}, and \texttt{SMILE\_SpecificListener.py}. On the
  Generalized side, the corresponding files are \texttt{SMILE\_GeneralizedLexer.py}, \texttt{SMILE\_GeneralizedParser.py}, and \texttt{SMILE\_GeneralizedListener.py}.

  \begin{figure}[htbp]
      \centering
      \resizebox{\textwidth}{!}{%
      \begin{tikzpicture}[
        font=\scriptsize,
        box/.style={draw, rounded corners=2pt, align=center, inner sep=5pt},
        file/.style={draw, rounded corners=2pt, align=center, inner sep=4pt, fill=gray!15},
        custom/.style={draw, rounded corners=2pt, align=center, inner sep=4pt, fill=yellow!15},
        arr/.style={-{Stealth[length=1.8mm]}, semithick},
        lbl/.style={font=\scriptsize, fill=white, inner sep=1pt},
      ]

        %% Row 1: .g4 files
        \node[box, fill=blue!12, minimum width=5cm] (g4s) at (-4, 0)
          {\textbf{SMILE\_Specific.g4}};
        \node[box, fill=blue!12, minimum width=5cm] (g4g) at (4, 0)
          {\textbf{SMILE\_Generalized.g4}};

        %% Row 2: ANTLR4 tool
        \node[box, fill=orange!15, minimum width=12cm] (antlr) at (0, -1.5)
          {\textbf{ANTLR~4 Code Generator} (\texttt{java -jar antlr-4.13.2-complete.jar -Dlanguage=Python3})};

        %% Row 3: Generated files - Specific (left column)
        \node[file, minimum width=5cm] (lexS) at (-4, -3.2)
          {\texttt{SMILE\_SpecificLexer.py}};
        \node[file, minimum width=5cm] (parS) at (-4, -4.2)
          {\texttt{SMILE\_SpecificParser.py}};
        \node[file, minimum width=5cm] (lisS) at (-4, -5.2)
          {\texttt{SMILE\_SpecificListener.py}};

        %% Row 3: Generated files - Generalized (right column)
        \node[file, minimum width=5cm] (lexG) at (4, -3.2)
          {\texttt{SMILE\_GeneralizedLexer.py}};
        \node[file, minimum width=5cm] (parG) at (4, -4.2)
          {\texttt{SMILE\_GeneralizedParser.py}};
        \node[file, minimum width=5cm] (lisG) at (4, -5.2)
          {\texttt{SMILE\_GeneralizedListener.py}};

        %% Labels for generated
        \node[font=\scriptsize\itshape, gray] at (-4, -5.9) {generated (do not modify)};
        \node[font=\scriptsize\itshape, gray] at (4, -5.9) {generated (do not modify)};

        %% Row 4: parser_factory.py
        \node[box, fill=green!12, minimum width=12cm] (pf) at (0, -7.2)
          {\texttt{parser\_factory.py} -- selects Lexer, Parser, Listener based on file extension (\texttt{.smile} / \texttt{.smile\_gen})};

        %% Row 5: Custom listeners
        \node[custom, minimum width=5cm] (cusS) at (-4, -8.7)
          {\texttt{SMILESpecificListener}};
        \node[custom, minimum width=5cm] (cusG) at (4, -8.7)
          {\texttt{SMILEGeneralizedListener}};
        \node[font=\scriptsize\itshape, gray] at (0, -9.4) {defined in \texttt{smile\_listeners.py}};

        %% === Arrows ===
        \draw[arr] (g4s.south) -- (antlr.north -| g4s.south);
        \draw[arr] (g4g.south) -- (antlr.north -| g4g.south);
        \draw[arr] (antlr.south -| lexS.north) -- (lexS.north);
        \draw[arr] (antlr.south -| lexG.north) -- (lexG.north);
        \draw[arr] (lisS.south) -- ++(0, -0.5) -| ([xshift=-2cm]pf.north);
        \draw[arr] (lisG.south) -- ++(0, -0.5) -| ([xshift=2cm]pf.north);
        \draw[arr] ([xshift=-2cm]pf.south) -- (cusS.north);
        \draw[arr] ([xshift=2cm]pf.south) -- (cusG.north);

        %% Inherits
        \draw[-{Triangle[open]}, semithick, dashed, gray]
          (cusS.west) -- ++(-1.2, 0) -- ++(0, 3.5) -- (lisS.west);
        \node[lbl, font=\scriptsize\itshape, rotate=90] at (-6.5, -7.0) {inherits};
        \draw[-{Triangle[open]}, semithick, dashed, gray]
          (cusG.east) -- ++(1.2, 0) -- ++(0, 3.5) -- (lisG.east);
        \node[lbl, font=\scriptsize\itshape, rotate=-90] at (6.5, -7.0) {inherits};

      \end{tikzpicture}
      }% end resizebox
      \caption{ANTLR~4 code generation output and integration with \texttt{parser\_factory.py}}
      \label{fig:antlr-codegen-detail}
  \end{figure}

  The generated files are not modified manually. Whenever a grammar rule is added or changed in a \texttt{.g4} file, the generation process is re-executed:

  \begin{lstlisting}[basicstyle=\small\ttfamily, caption={ANTLR~4 generation commands}, label={lst:antlr-command}]
  cd grammar/specific
  java -jar ../antlr-4.13.2-complete.jar -Dlanguage=Python3 SMILE_Specific.g4

  cd grammar/generalized
  java -jar ../antlr-4.13.2-complete.jar -Dlanguage=Python3 SMILE_Generalized.g4
  \end{lstlisting}

The separation between generated and project code is strict. This follows the listener pattern recommended by the ANTLR~4 documentation\footnote{The ANTLR~4
  documentation describes this pattern as follows: ``ANTLR generates a listener interface that makes it easy to respond to the recognition of phrases of interest. You
  can build your listener by subclassing the base and overriding the methods of interest.'' See \url{https://github.com/antlr/antlr4/blob/master/doc/listeners.md}.},
  where the generated base class serves as the interface contract and the custom subclass provides the implementation. The generated listener base classes, such as
  \texttt{SMILE\_SpecificListener.py}, contain only empty callback methods that define the interface but perform no action. The custom classes
  \texttt{SMILESpecificListener} and \texttt{SMILEGeneralizedListener} in \texttt{smile\_listeners.py} inherit from these generated base classes and override the callbacks   with the actual logic that extracts operation parameters from the parse tree. As a result, grammar changes only require re-running the ANTLR tool, while the
  interpretation logic remains independent. In practice, adding a new grammar rule involves defining it in the \texttt{.g4} file and implementing the corresponding
  \texttt{enter*} method in \texttt{smile\_listeners.py}, without modifying the generated code.

  \subsection{Parser Factory}\label{subsec:parser-factory}
The entry point for the entire syntax analysis is the function \texttt{parse\_smile\_auto(file\_path)} in \texttt{parser\_factory.py}. It is called by \texttt{core.py}
  with a single file path and returns a triplet \texttt{(context, operations, errors)}, which contains the parsed script header, the ordered operation list, and any
  syntax errors.

\autoref{fig:parser-factory-flow} summarizes the five internal steps of the function. Grammar detection and component selection are separated into two distinct steps
  so that adding a new grammar variant would only require registering its classes in \texttt{get\_parser\_components()}, without changing the parsing logic itself. Based   on the file extension, \texttt{detect\_grammar\_type()} selects either the Specific (\texttt{.smile}) or the Generalized (\texttt{.smile\_gen}) variant, and
  \texttt{get\_parser\_components()} returns the corresponding Lexer and Parser classes. The input text is then processed through the standard ANTLR~4 chain:
  \texttt{FileStream} $\to$ Lexer $\to$ \texttt{CommonTokenStream} $\to$ Parser $\to$ parse tree. In the final step, a \texttt{ParseTreeWalker} traverses the parse tree
  with the selected Listener, which produces the operation list as described in \autoref{subsec:listeners}.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
      font=\scriptsize,
      box/.style={draw, rounded corners=2pt, align=center, inner sep=5pt,
                  minimum width=5cm, minimum height=0.7cm},                                                                                                                    arr/.style={-{Stealth[length=1.8mm]}, semithick},
    ]

      %% Input
      \node[box, fill=blue!12] (input) at (0, 0)
        {\textbf{Input}\\
         SMILE script file (\texttt{.smile} or \texttt{.smile\_gen})};

      %% Step 1: detect
      \node[box, fill=green!12] (detect) at (0, -2.0)
        {\textbf{Grammar Detection}\\
         \texttt{detect\_grammar\_type(file\_path)}};

      %% Step 2: select
      \node[box, fill=green!12] (select) at (0, -4.0)
        {\textbf{Component Selection}\\
         \texttt{get\_parser\_components(grammar\_type)}};

      %% Step 3: parse chain
      \node[box, fill=orange!12] (chain) at (0, -6.0)
        {\textbf{Tokenization and Parsing}\\
         \texttt{FileStream} $\to$ \texttt{Lexer} $\to$ \texttt{CommonTokenStream} $\to$ \texttt{Parser}};

      %% Step 4: tree
      \node[box, fill=orange!12] (tree) at (0, -8.0)
        {\textbf{Parse Tree Construction}\\
         \texttt{parser.migration()}};

      %% Step 5: walk
      \node[box, fill=yellow!15] (walk) at (0, -10.0)
        {\textbf{Tree Traversal}\\
         \texttt{ParseTreeWalker().walk(listener, tree)}};

      %% Output
      \node[box, fill=red!10] (output) at (0, -12.0)
        {\textbf{Output}\\
         \texttt{(context, operations, errors)}};

      %% Arrows
      \draw[arr] (input.south) -- (detect.north);
      \draw[arr] (detect.south) -- (select.north);
      \draw[arr] (select.south) -- (chain.north);
      \draw[arr] (chain.south) -- (tree.north);
      \draw[arr] (tree.south) -- (walk.north);
      \draw[arr] (walk.south) -- (output.north);

    \end{tikzpicture}
    \caption{Internal processing steps of \texttt{parse\_smile\_auto()} in \texttt{parser\_factory.py}}
    \label{fig:parser-factory-flow}
  \end{figure}
For the calling code in \texttt{core.py}, \texttt{run\_migration()} passes the file path and receives a uniform operation list back, regardless of which grammar variant was used. The grammar distinction is fully encapsulated within \texttt{parser\_factory.py}.


  \subsection{Listener-based Parse Tree Processing}\label{subsec:listeners}

  This section describes the last step of the processing chain. The listener                                                                                               classes map the nodes of the parse tree into three data structures defined
  in \texttt{smile\_listeners.py} (\autoref{fig:listener-output}).
  \texttt{MigrationContext} records the information from the script header,                                                                                                including the script name, the version, the source and target database                                                                                                   type, and the distinction between migration and evolution via the field
  \texttt{is\_evolution}. For evolution scripts, the additional field
  \texttt{target\_schema\_version} contains the target version from the
  \texttt{VERSION x TO y} declaration. \texttt{OpType} is an enumeration
  with 30~values that identifies each operation type. \texttt{Operation}
  combines an \texttt{OpType} with a typed parameter record whose concrete
  class depends on the operation. The 30 parameter classes are defined in
  \texttt{operation\_params.py} as \texttt{@dataclass}es (for example
  \texttt{NestParams}, \texttt{UnnestParams}, \texttt{AddPropertyParams}),
  together with a \texttt{Union} alias \texttt{OpParams} used to annotate
  the \texttt{Operation.params} field. This turns the parser-transformer
  contract into a statically checked interface: a field-name mismatch
  between a listener that builds, say, a \texttt{NestParams} and a handler
  that expects one becomes a static type error rather than a silent
  \texttt{params.get(\ldots)} returning \texttt{None} and the operation
  being skipped at runtime.

  \begin{figure}[t]
      \centering
      \begin{tikzpicture}[
        font=\scriptsize,
        box/.style={draw, rounded corners=2pt, align=left, inner sep=5pt,
                    minimum width=4.5cm, minimum height=4.8cm},
        arr/.style={-{Stealth[length=1.8mm]}, semithick},
        lbl/.style={font=\scriptsize, fill=white, inner sep=1pt},
      ]

        %% Parse Tree (input)
        \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
              fill=orange!12, minimum width=12cm, minimum height=0.7cm] (tree) at (0, 0)
          {\textbf{Parse Tree}\\
           produced by \texttt{parser.migration()}};

        %% Walker
        \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
              fill=yellow!15, minimum width=12cm, minimum height=0.7cm] (walker) at (0, -1.8)
          {\textbf{ParseTreeWalker} triggers \texttt{enter*} callbacks};

        %% Two listeners side by side
        \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
              fill=yellow!8, minimum width=5.5cm, minimum height=0.7cm] (lisS) at (-3, -3.6)
          {\texttt{SMILESpecificListener}\\
           inherits \texttt{SMILE\_SpecificListener}};
        \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
              fill=yellow!8, minimum width=5.5cm, minimum height=0.7cm] (lisG) at (3, -3.6)
          {\texttt{SMILEGeneralizedListener}\\
           inherits \texttt{SMILE\_GeneralizedListener}};

        %% BaseSMILEListener
        \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
              fill=green!15, minimum width=12cm, minimum height=0.7cm] (base) at (0, -5.4)
          {\textbf{BaseSMILEListener} (shared helper methods)\\
           \texttt{\_parse\_condition\_pairs()},
           \texttt{\_parse\_property\_clauses()},
           \texttt{\_parse\_entity\_clauses()}, \ldots};

        %% Three outputs
        \node[box, fill=green!10] (ctx) at (-4.5, -9.4)
          {\textbf{MigrationContext}\\[3pt]
           \texttt{name: str}\\
           \texttt{version: str}\\
           \texttt{is\_evolution: bool}\\
           \texttt{source\_db\_type: str}\\
           \texttt{target\_db\_type: str}\\
           \texttt{schema\_name: str}\\
           \texttt{schema\_version: str}\\
           \texttt{target\_schema\_version: str}\\[3pt]
           \textit{is\_evolution = false} $\to$ \textit{Migration}\\
           \textit{is\_evolution = true} $\to$ \textit{Evolution}};

        \node[box, fill=blue!10] (optype) at (0, -9.4)
          {\textbf{OpType} (30 values)\\[3pt]
           \texttt{NEST, UNNEST}\\
           \texttt{FLATTEN, UNFLATTEN}\\
           \texttt{WIND, UNWIND}\\
           \texttt{ADD\_ENTITY}\\
           \texttt{ADD\_PROPERTY}\\
           \texttt{CAST\_CONSTRAINT}\\
           \texttt{TRANSFORM}\\
           \texttt{\ldots}};

        \node[box, fill=red!10] (op) at (4.5, -9.4)
          {\textbf{Operation}\\[3pt]
           \texttt{op\_type: OpType}\\
           \texttt{params: OpParams}\\
           \texttt{original\_keyword: str}\\[6pt]
           \textit{Example:}\\
           \texttt{op\_type = ADD\_PROPERTY}\\
           \texttt{params = AddPropertyParams(}\\
           \texttt{~~name="city",}\\
           \texttt{~~entity="products",}\\
           \texttt{~~clauses=[...])}};

        %% === Arrows ===
        \draw[arr] (tree.south) -- (walker.north);
        \draw[arr] (walker.south) -- ++(0, -0.3) -| (lisS.north);
        \draw[arr] (walker.south) -- ++(0, -0.3) -| (lisG.north);
        \draw[-{Triangle[open]}, semithick, dashed, gray] (lisS.south) -- (base.north -| lisS.south);
        \draw[-{Triangle[open]}, semithick, dashed, gray] (lisG.south) -- (base.north -| lisG.south);
        \node[lbl, font=\scriptsize\itshape] at (-5.5, -4.5) {inherits};
        \node[lbl, font=\scriptsize\itshape] at (5.5, -4.5) {inherits};
        \draw[arr] (base.south) -- ++(0, -0.8) -| (ctx.north);
        \draw[arr] (base.south) -- ++(0, -0.8) -- (optype.north);
        \draw[arr] (base.south) -- ++(0, -0.8) -| (op.north);
        \node[lbl, font=\scriptsize\itshape] at (0, -6.8) {produces};

      \end{tikzpicture}
      \caption{Listener class hierarchy and output data structures in \texttt{smile\_listeners.py}}
      \label{fig:listener-output}
  \end{figure}

  As shown in \autoref{fig:listener-output}, both listeners need to handle
  the same recurring parsing tasks. These methods are placed in a shared
  base class \texttt{BaseSMILEListener}, for example
  \texttt{\_parse\_condition\_pairs()} extracts \texttt{WHERE} join
  conditions, \texttt{\_parse\_property\_clauses()} processes
  \texttt{WITH TYPE} declarations, and \texttt{\_parse\_entity\_clauses()}
  handles property definition lists. \texttt{SMILESpecificListener} inherits
  from the generated \texttt{SMILE\_SpecificListener} together with
  \texttt{BaseSMILEListener}. \texttt{SMILEGeneralizedListener} follows the
  same pattern. Both override the \texttt{enter*} callbacks to extract
  operation parameters from the parse tree and to wrap them in the
  OpType-specific dataclass.

  Although the two grammars use different syntactic forms, both listeners
  produce identical \texttt{Operation} objects with the same \texttt{OpType}
  value and the same typed parameter record. For example,
  \texttt{ADD\_PROPERTY name TO products WITH TYPE String} in the Specific
  grammar and \texttt{ADD PROPERTY name TO products WITH TYPE String} in
  the Generalized grammar are each translated into an \texttt{Operation}
  with \texttt{op\_type = OpType.ADD\_PROPERTY} and an identical
  \texttt{AddPropertyParams} instance. The Schema Transformer in
  \autoref{sec:transformer} therefore receives the same input, regardless
  of which grammar variant was used.


\section{Schema Transformer}\label{sec:transformer}

This section covers the execution of operations on the M-Model+ database
object. The class \texttt{SchemaTransformer} in \texttt{core.py} receives
the operation list together with a copy of Meta Schema V1 and applies
each operation sequentially through a handler dispatch mechanism, as
shown in \autoref{fig:transformer-overview}. The handlers correspond to
the operation taxonomy defined in \autoref{sec:operation-taxonomy} and
are organized into the same five categories.
\autoref{subsec:transformer-arch} first describes the dispatch
architecture that is shared by all handlers, followed by the
implementation of each category.

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
      font=\scriptsize,
      box/.style={draw, rounded corners=2pt, align=center, inner sep=5pt,
                  minimum width=4.5cm, minimum height=0.7cm},
      cat/.style={draw, rounded corners=2pt, align=center, inner sep=3pt,
                  fill=orange!10, minimum width=1.8cm, minimum height=0.5cm},
      arr/.style={-{Stealth[length=1.8mm]}, semithick},
    ]
      %% Two inputs
      \node[box, fill=red!10] (ops) at (-3, 0)
        {\textbf{Operation List}\\
         (from parsing layer)};
      \node[box, fill=blue!10] (v1) at (3, 0)
        {\textbf{Meta Schema V1}\\
         (copy from source adapter)};

      %% SchemaTransformer
      \node[draw, rounded corners=2pt, align=center, inner sep=5pt,
            fill=yellow!15, minimum width=10cm, minimum height=0.7cm] (transformer) at (0, -1.8)
        {\textbf{SchemaTransformer}\\
         dispatches each operation to its handler};

      %% 5 handler categories
      \node[cat] (h1) at (-4, -3.8) {Entity \&\\Property};
      \node[cat] (h2) at (-2, -3.8) {Structural};
      \node[cat] (h3) at (0, -3.8) {Key \&\\Constraint};
      \node[cat] (h4) at (2, -3.8) {Relationship};
      \node[cat] (h5) at (4, -3.8) {Graph};

      %% Output
      \node[box, fill=green!12, minimum width=10cm] (v2) at (0, -5.6)
        {\textbf{Meta Schema V2}};

      %% Arrows: inputs -> transformer
      \draw[arr] (ops.south) -- (transformer.north -| ops.south);
      \draw[arr] (v1.south) -- (transformer.north -| v1.south);

      %% Arrows: transformer -> handler categories
      \coordinate (tfork) at (0, -2.7);
      \draw[semithick] (transformer.south) -- (tfork);
      \draw[arr] (tfork) -| (h1.north);
      \draw[arr] (tfork) -| (h2.north);
      \draw[arr] (tfork) -- (h3.north);
      \draw[arr] (tfork) -| (h4.north);
      \draw[arr] (tfork) -| (h5.north);

      %% Arrows: handler categories -> output
      \coordinate (outfork) at (0, -4.8);
      \draw[semithick] (h1.south) |- (outfork);
      \draw[semithick] (h2.south) |- (outfork);
      \draw[semithick] (h3.south) -- (outfork);
      \draw[semithick] (h4.south) |- (outfork);
      \draw[semithick] (h5.south) |- (outfork);
      \draw[arr] (outfork) -- (v2.north);
    \end{tikzpicture}
    \caption{SchemaTransformer dispatch architecture in \texttt{core.py}}
    \label{fig:transformer-overview}
\end{figure}


\subsection{Architecture and Operation Dispatch}\label{subsec:transformer-arch}

  The class \texttt{SchemaTransformer} in \texttt{core.py}, which forms the
  central component of the core layer, consumes Meta Schema~V1 through its
  constructor and produces Meta Schema~V2 by applying the operation list one
  step at a time. Because every handler mutates the schema in place, the
  constructor first deep-copies the input (\texttt{copy.deepcopy()}), leaving
  the caller's original object intact. Before handing the source over to the
  transformer, the driver function (\texttt{run\_migration()}) also retains
  its own reference copy (\texttt{meta\_v1\_db}) of the V1 schema, which the
  web interface later displays next to the V2 result so that the user can
  trace every change side by side.

  While the operations are being applied, the transformer maintains two
  pieces of state. A transformation log (\texttt{changes}) appends a short
  label for each handler effect (for instance,
  \texttt{"ADD\_KEY:customers.(customer\_id)"} or
  \texttt{"UNWIND:order\_details"}), which the web interface later queries
  to summarize what the run produced, such as identifying the entities
  created along the way. A source-side key snapshot
  (\texttt{source\_key\_snapshot}) is initialized from the source schema
  and records the primary key of each entity (field name, data type,
  prefix) at construction time; the web interface queries it (exported
  under the JSON key \texttt{"key\_registry"} for backward compatibility)
  to show key information per entity and to resolve the targets of
  foreign-key references. Once every operation has been executed, a closing
  normalization pass (\texttt{\_normalize\_entity\_kinds()}) rewrites every
  entity to the target paradigm's default kind, for instance \texttt{TABLE}
  for relational or \texttt{VERTEX} for graph.

  Sequential execution is driven by a handler registry (\texttt{\_handlers}),
  a dictionary mapping each of the 30 \texttt{OpType} values to a
  corresponding \texttt{\_handle\_*} method (\autoref{lst:handler-registry}).
  This realizes the Command pattern, where \texttt{OpType} serves as the
  command identifier, \texttt{params} (a typed dataclass from
  \texttt{operation\_params.py}) as its payload, and the method as the
  receiver. The pattern is a standard Python idiom~\cite{ramalho2022fluent}
  that selects the right handler with a single dictionary lookup, avoiding a
  long \texttt{if}/\texttt{elif} chain, and lets new operations be registered
  by adding a single line.

  \begin{lstlisting}[language=Python, basicstyle=\small\ttfamily,
    caption={Handler registry in \texttt{SchemaTransformer.\_\_init\_\_} (excerpt)},
    label={lst:handler-registry}]
  class SchemaTransformer:
      def __init__(self, database: Database):
          self.database = copy.deepcopy(database)
          self.changes: List[str] = []
          self.source_key_snapshot: Dict[str, Dict[str, Any]] = {}
          self._init_source_keys()

          self._handlers = {
              # Structural
              OpType.NEST:       self._handle_nest,
              OpType.UNNEST:     self._handle_unnest,
              OpType.FLATTEN:    self._handle_flatten,
              ...
              # Entity & Property
              OpType.ADD_ENTITY:    self._handle_add_entity,
              OpType.RENAME_ENTITY: self._handle_rename_entity,
              ...
              # Key & Constraint / Relationship & Graph
              OpType.ADD_KEY:    self._handle_add_key,
              OpType.TRANSFORM:  self._handle_transform,
              OpType.RECARD:     self._handle_recard,
          }
  \end{lstlisting}

  Every handler exposes the same shape. It takes a single typed parameter
  record (the OpType-specific dataclass from \texttt{operation\_params.py},
  for example \texttt{NestParams} for \texttt{NEST}) and returns a boolean,
  where \texttt{False} indicates a skipped operation, for instance when the
  target entity does not exist. The dispatch loop in
  \texttt{run\_migration()} (\autoref{lst:dispatch-loop}) relies on this
  uniformity. For each operation, the loop takes a snapshot of the
  database, dispatches it to the handler returned by
  \texttt{\_handlers.get()}, and takes a second snapshot once the call
  returns. A diff helper (\texttt{\_calculate\_changes()}) compares the two
  snapshots and derives the change record for this step, which the web
  interface uses for its visualization. Two counters
  (\texttt{success\_count} and \texttt{skipped\_count}) accumulate the
  outcomes of all handler calls and produce the final summary of the run.

  \begin{lstlisting}[language=Python, basicstyle=\small\ttfamily,
    caption={Dispatch loop in \texttt{run\_migration()} (excerpt)},
    label={lst:dispatch-loop}]
  for op in operations:
      prev = db_to_dict(transformer.database)
      handler = transformer._handlers.get(op.op_type)
      try:
          success = handler(op.params) if handler else False
      except Exception:
          success = False
      success_count += int(bool(success))
      skipped_count += int(not success)
      changes_detail = _calculate_changes(
          prev, db_to_dict(transformer.database), op)
  \end{lstlisting}

  Alongside the dispatch mechanism, the transformer draws on a small set of
  shared utilities. Three module-level dictionaries in
  \texttt{unified\_meta\_schema.py} centralize the conversion of SMILE
  string literals into internal enumerations, so that a handler (and any
  other module needing the same mapping, such as a validator) resolves
  them with one lookup rather than a chain of conditionals. They cover
  primitive type names (mapped to \texttt{PrimitiveType}), cardinality
  strings (mapped to the \texttt{Cardinality} enum of
  \autoref{subsec:meta-data-structures}), and key identifiers (mapped to
  their internal representations, including the Cassandra-specific
  \texttt{PKTypeEnum} values). Three helper methods on the transformer
  then smooth navigation over the \texttt{Database} object. One looks up
  an entity by name and reports a notice if it is missing, another splits
  a dotted string such as \texttt{"orders.order\_date"} into its
  components, and a third combines the two to yield an
  \texttt{(entity, attribute)} pair.


\subsection{Entity and Property Handlers}\label{subsec:transformer-entity-prop}


\subsection{Structural Transformation Handlers}\label{subsec:transformer-structural}


\subsection{Key and Constraint Handlers}\label{subsec:transformer-keys}


\subsection{Relationship and Graph Handlers}\label{subsec:transformer-rel-graph}


\section{Database Adapters}\label{sec:adapters}
\subsection{Adapter Architecture}\label{subsec:adapter-arch}
\subsection{PostgreSQL Adapter}\label{subsec:adapter-pg}
\subsection{MongoDB Adapter}\label{subsec:adapter-mongo}
\subsection{Neo4j Adapter}\label{subsec:adapter-neo4j}
\subsection{Cassandra Adapter}\label{subsec:adapter-cassandra}

\section{Validation}\label{sec:validation}
\subsection{Meta Schema Validation}\label{subsec:validate-meta}
\subsection{Round-trip Validation}\label{subsec:validate-roundtrip}

\section{User Interface}\label{sec:user-interface}

\section{Summary}\label{sec:impl-summary}
